{
  "install": "pip install -r requirements.txt && bash scripts/install_agent.sh && python3 -c \"from gex_core.env_bootstrap import bootstrap_env; bootstrap_env()\"",
  "start": "bash scripts/start_web.sh",
  "ports": [
    {
      "name": "web",
      "port": 8080
    }
  ]
}
